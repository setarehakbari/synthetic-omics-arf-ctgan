# Data

The real gene-expression dataset used in this study may be subject to access and/or sharing restrictions.

- This repository provides the full preprocessing and evaluation pipeline.
- To reproduce results, place the authorized real dataset file(s) in `data/real/` (not tracked by git).
- Synthetic outputs and derived summary tables can be generated by running the scripts in the repository.
